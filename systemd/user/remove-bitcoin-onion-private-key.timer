[Unit]
Description=Remove the Bitcoin Core server's private Onion key to force it to generate a new key.

[Service]
Type=oneshot
ExecStart=/usr/bin/rm --force /var/lib/bitcoin/onion_v3_private_key

[Install]
WantedBy=default.target